name: Update Documentation API Files

on:
  workflow_dispatch:
  push:
    tags:
      - 'v*' # vで始まるすべてのタグでワークフローをトリガー

jobs:
  setup:
    # ⬇️ この行を追加
    # タグ名に 'rc' が含まれていない場合のみ、このジョブを実行
    if: "!contains(github.ref_name, 'rc')"

    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Run job steps
        run: echo "Running job for tag ${{ github.ref_name }} because it's not a release candidate."
